JS proxy সার্ভার ফাইলগুলো C:\Users\sahon\local\Proxy Server ফোল্ডারে রাখে

গিট কমেন্ট করে

মেইন এজেন্ট সার্ভারের সাথে কানেক্ট করে

একটা পূর্ণাঙ্গ, স্থির, লোকাল-অনলি সিস্টেম বানিয়ে

মডেল লোড, রেসপন্স, ফ্লো—সব পরীক্ষা করে

এডিটরের config ঠিক করে

এবং runtime-এ দেখতে পারে ঠিকভাবে কাজ করছে কিনা

আমি এখনই এজেন্টকে দেওয়ার মতো স্পষ্ট, কঠোর, নিখুঁত নির্দেশনা লিখে দিচ্ছি—
যেন তোর লোকাল এজেন্ট (Python) ঠিক বুঝে একে একে কাজগুলো করে ফেলে।

✅ এজেন্টের জন্য অফিসিয়াল নির্দেশনা (তুই কপি করে unified_agent_system.py শুরুতে বা config-এ দিতে পারিস)

এই ব্লকটা এজেন্টের জন্য, মানুষের জন্য না।
এজেন্ট এই নির্দেশ মানলে পুরো সিস্টেম perfect হবে।

🔥 AGENT DIRECTIVE / SYSTEM INSTRUCTIONS (Copy-Paste করে Agent-এ দে)
# ============ ZOMBIECODER LOCAL AGENT SYSTEM DIRECTIVE ============
# The agent MUST follow all instructions below exactly.

1) Proxy Server Integration:
   - All proxy server files must be located under:
       C:\Users\sahon\local\Proxy Server
   - Required files:
       proxy_server_enforced.js
       proxy_server.js (optional fallback)
       package.json
   - If missing, create them exactly as previously provided.

2) Git Versioning:
   - Before starting any service, ALWAYS run:
       git add .
       git commit -m "auto: update before start"
   - NEVER skip this step.

3) Service Linking:
   - The agent MUST ensure:
       Proxy runs on port 5010
       Main agent runs on port 8001
       No cloud endpoints or fallbacks are ever used.

4) Editor Support:
   - The agent MUST expose:
       /v1/models
       /v1/models/microsoft/phi-2
       /v1/chat/completions (OpenAI-compatible)
     If missing, the proxy must shim these endpoints.

5) Model Handling:
   - Auto-load "microsoft/phi-2" on agent startup.
   - No repeated downloads.
   - No cloud fallback.
   - If model not found → load from local storage.

6) Startup Order:
   Step 1) Commit changes
   Step 2) Start Python agent
   Step 3) After 3 seconds, start Proxy Server
   Step 4) Verify:
        GET http://127.0.0.1:8001/health
        GET http://127.0.0.1:5010/health

7) Stability Requirements:
   - Prevent reload loops
   - Serve instant response to editor
   - Enforce Bengali-friendly system prompt
   - Detect and block malformed JSON
   - Log every request in logs/proxy_out.log

8) Final Responsibility:
   - After every change, agent MUST:
        Validate ports 5010 and 8001
        Validate "model_ready:true"
        Validate editor can connect
   - The system must run fully offline.
   - Any cloud communication → MUST be blocked and logged.

# ============ END OF DIRECTIVE ============

✅ এজেন্টকে এখন কী করতে বলবি (মানুষ হিসেবে তুই)

এজেন্টকে বল:

🔊 "Agent, follow the SYSTEM DIRECTIVE above and do the following now:"

C:\Users\sahon\local\Proxy Server ফোল্ডার ক্রিয়েট করো

proxy_server_enforced.js, proxy_server.js, package.json সেখানে রাখো

git init না থাকলে init করো

git add & commit চালাও

Python main agent স্টার্ট করো

3 সেকেন্ড অপেক্ষা করো

Proxy সার্ভার স্টার্ট করো (node দিয়ে)

/v1/models এবং /v1/chat/completions—সব endpoint Active আছে কিনা দেখো

Editor config সঠিকভাবে আপডেট করো:

Base URL → http://127.0.0.1:5010/v1

Model → microsoft/phi-2

API Key → empty

স্বাস্থ্য পরীক্ষা চালাও (health check)

Report দাও:

Proxy running

Agent running

Model loaded

Editor connected

No cloud fallback